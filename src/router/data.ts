export enum PageType {
  'webapp' = 1, //webapp
  'ds_view' = 2, //表视图
  'generate_query' = 3, //通用查询
  'ds_query' = 4, //表查询,
  'data_kanban' = 5, //数据看板,
  'external_link' = 6, //外链
}

export enum LayoutType {
  'single_tab' = 1, //单页
  'design_tab' = 2, //多tab
}
